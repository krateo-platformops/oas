{
  "$schema": "http://json-schema.org/draft-07/schema",
  "type": "object",
  "title": "krateo-oas",
  "description": "Singleton registry of merged Krateo API-Builder RestDefinitions, rendered as live cluster resources. No per-instance configuration.",
  "additionalProperties": false,
  "properties": {
    "global": {
      "type": "object",
      "title": "Global values",
      "description": "Helm global values (injected when used as a subchart)."
    }
  }
}
